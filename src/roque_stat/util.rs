use ndarray::Array2;

// pub trait Util {
//   fn ensure_diag_above_threshold(sigma: &Array2<f64>, threshold: f64, n_dim: usize) -> &Array2<f64> {
//     sigma
//   }
// }

pub(crate) fn ensure_diag_above_threshold(sigma: &Array2<f64>, threshold: f64, n_dim: usize) -> &Array2<f64> {
  sigma
}
